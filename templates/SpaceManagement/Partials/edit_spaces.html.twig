
<div class="white"> 
        <div class="container"> 
<div class="bkg-img"></div>
     
    <div class="row top-profil">
        <!-- BEGIN MAIN CONTENT -->
        <div class=" col-sm-12">
            <div class="titre-page">
        <h1>
          {{ 'Ajouter un espace' }}
        </h1>
         <div class="img-aac"><img src="{{ asset('images/groupe_ajouter_espace.png') }}" alt="Plateau Urbain" /></div>
         </div>
            </div>
            <div class="col-sm-8">
            <p class="left">
            Pour ajouter une offre sur la plate-forme Plateau urbain, il vous suffit de remplir le formulaire ci-dessus.<br /> A tout moment, vous pouvez enregistrer le formulaire et reprendre son édition ultérieurement. Une fois complet, procédez à sa validation : un administrateur du site interviendra pour s’assurer qu’elle est complète et procéder à sa mise en ligne.
            </p>     
    </div>
</div>

{% if app.flashes|length %}
<div class="container" style="margin-bottom: 10px;">
    {% for label, messages in app.flashes %}
        {% for message in messages %}
            <div class="alert alert-{{ label == 'error' ? 'danger' : (label == 'success' ? 'success' : (label == 'warning' ? 'warning' : 'info')) }} alert-dismissible" role="alert">
                <button type="button" class="close" data-dismiss="alert" aria-label="Fermer"><span aria-hidden="true">&times;</span></button>
                {{ message }}
            </div>
        {% endfor %}
    {% endfor %}
</div>
{% endif %}

{% form_theme form 'Form/bootstrap_3_layout.html.twig' %}
<style>
    /* Le fond du formulaire n'étant pas garanti (variable selon la section),
       le message d'erreur porte son propre fond pour rester lisible dans tous les cas. */
    #js-form-space .has-error .help-block {
        display: block;
        color: #FFF;
        background-color: #e9473c;
        font-size: 13px;
        margin-top: 4px;
        padding: 6px 10px;
        border-radius: 4px;
    }
    #js-form-space .has-error .help-block ul { margin: 0; }
    #js-form-space .has-error .form-control,
    #js-form-space .has-error select.form-control {
        border-color: #e9473c;
    }
    #js-form-space .trumbowyg-box,
    #js-form-space .trumbowyg-editor-box { border: none; }
</style>
{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}

<div class="row">
        <div class=" col-xs-1 bkg ">
            <ul>
                <li>
                    <a class="indicator active" href="#one"></a>
                </li>
                <li>
                    <a class="indicator" href="#two"></a>
                </li>
                <li>
                    <a class="indicator" href="#three"></a>
                </li>
                <li>
                    <a class="indicator" href="#four"></a>
                </li>
                <li>
                    <a class="indicator" href="#five"></a>
                </li>
                
            </ul>
        </div>     

<div class="main col-xs-10">
    <div id="one" class="section">
        <span class="section-title form-one"></span>
        <div  class="form-contener bleu">
            <h3>Votre espace</h3>
         
         {{ form_errors(form) }}



     
             <div class="row">
                <div class="col-md-6 col-sm-12 col-xs-12 form-group{% if not form.name.vars.valid %} has-error{% endif %}">
                    {{ form_label(form.name) }}
                    {{ form_widget(form.name) }}
                    {{ form_errors(form.name) }}
                </div>
                <div class="col-md-6 col-sm-12 col-xs-12 form-group{% if not form.managedByLabel.vars.valid %} has-error{% endif %}">
                    {{ form_label(form.managedByLabel) }}
                    {{ form_widget(form.managedByLabel) }}
                    {{ form_errors(form.managedByLabel) }}
                </div>
            </div>


            <div class="row">
                <div class="col-md-6 col-sm-12 col-xs-12 form-group{% if not form.zipCode.vars.valid %} has-error{% endif %}">
                    {{ form_label(form.zipCode) }}
                    {{ form_widget(form.zipCode, {'attr': {'class': 'form-control', 'inputmode': 'numeric', 'autocomplete': 'postal-code'}}) }}
                    {{ form_errors(form.zipCode) }}
                </div>
                <div class="col-md-6 col-sm-12 col-xs-12 form-group{% if not form.city.vars.valid %} has-error{% endif %}">
                    {{ form_label(form.city) }}
                    {{ form_widget(form.city) }}
                    {{ form_errors(form.city) }}
                </div>
            </div>

            <div class="row">
                <div class="col-md-6 col-sm-12 col-xs-12 form-group{% if not form.limitAvailability.vars.valid %} has-error{% endif %}">
                    {{ form_label(form.limitAvailability) }}
                    <div class="row">
                        <div class="col-xs-12 col-sm-6 form-group">
                            {{ form_widget(form.limitAvailability.date, {'attr': {'class': 'form-control'}}) }}
                        </div>
                        <div class="col-xs-6 col-sm-3 form-group">
                            {{ form_widget(form.limitAvailability.time.hour) }}
                        </div>
                        <div class="col-xs-6 col-sm-3 form-group">
                            {{ form_widget(form.limitAvailability.time.minute) }}
                        </div>
                    </div>
                    {{ form_errors(form.limitAvailability) }}
                </div>
                <div class="col-md-6 col-sm-12 col-xs-12 form-group{% if not form.availability.vars.valid %} has-error{% endif %}">
                    {{ form_label(form.availability) }}
                    {{ form_widget(form.availability) }}
                    {{ form_errors(form.availability) }}
                </div>
            </div>

            <div class="row">
                <div class="col-sm-12 form-group">
                    {{ form_row(form.type) }}
                </div>
               
            </div>

         <div class="row" style="display: flex; align-items: center;">
            <div class="col-md-5 col-sm-12 col-xs-12 form-group">
                {{ form_row(form.price) }}
        </div>
        <div class="col-sm-2 text-center form-group ou-separator">
                <span>ou</span>
        </div>
        <div class="col-md-6 col-sm-12 col-xs-12 form-group">
            {{ form_row(form.priceText, { 'attr': { 'placeholder': 'Lorem ipsum' } }) }}
        </div>
</div>

            <div class="row">
                <div class="col-sm-4 form-group">
                    {{ form_row(form.nbSpaces) }}
                </div>
                <div class="col-sm-4 form-group">
                    {{ form_row(form.minSpace) }}
                </div>
                <div class="col-sm-4 form-group">
                    {{ form_row(form.maxSpace) }}
                </div>
            </div>


            <div class="row">
                <div class="col-sm-12 form-group{% if not form.description.vars.valid %} has-error{% endif %}">
                    {{ form_label(form.description) }}
                    {{ form_widget(form.description) }}
                    {{ form_errors(form.description) }}
                </div>
            </div>

            <div class="row form-group">
                <div class="col-sm-12 form-group{% if not form.activityDescription.vars.valid %} has-error{% endif %}">
                    {{ form_label(form.activityDescription) }}
                    {{ form_widget(form.activityDescription) }}
                    {{ form_errors(form.activityDescription) }}
                </div>
            </div>

            {% if form.isErp is defined %}
            <div class="row">
                <div class="col-sm-12 form-group">
                    <div class="alert alert-info" role="alert" style="margin-top: 8px;">
                        <strong>ERP (Établissement Recevant du Public) :</strong> activer si le site est ouvert au public.
                        Certains "types d’usage" réservés aux ERP seront alors proposés aux candidats.
                    </div>
                    {{ form_row(form.isErp) }}
                </div>
            </div>
            {% endif %}
        </div>
    </div>

    

    <div id="two" class="section">
        <span class="section-title form-two"></span>
        <div class="form-contener bleu">
            <h3>
                Photos du site
            </h3>
            <div class="row">
                <div class="col-sm-12">
                    {% include 'SpaceManagement/Partials/_space_images_form.html.twig' with { 'images': form.vars.data.pics,'form': form.pics } %}
                </div>
            </div>
        </div>
    </div>

    <div id="three" class="section">
        <span class="section-title form-three"></span>
        <div class="form-contener bleu">
            <h3>
                Pièce(s) demandée(s)
            </h3>
            <div class="row">
                <div class="col-sm-12">
                    {% include 'SpaceManagement/Partials/_space_documents_form.html.twig' with { 'documents': form.vars.data.documents,'form': form.newDocument } %}
                </div>
            </div>
        </div>
    </div>

    <div id="four" class="section">
        <span class="section-title form-four"></span>
        <div class="form-contener bleu">
            <h3>
                Documents ressources
            </h3>
            <div class="row">
                <div class="col-sm-12">
                {% include 'SpaceManagement/Partials/_space_required_form.html.twig' with { 'form': form, 'space': space } %}
               
                </div>
            </div>
        </div>
    </div>

    <div id="five" class="section">
        <span class="section-title form-five"></span>
        <div class="form-contener bleu">
            <h3>
                Visites organisées
            </h3>
            <div class="row">
                <div class="col-sm-12">
                    {% include 'SpaceManagement/Partials/_space_visits_form.html.twig' with { 'visits': form.vars.data.visits,'form': form.newVisit } %}
                </div>
            </div>
        </div>
    </div>



        <div class="row btns">
            <div class="col-sm-12">
                {{ form_widget(
                    form.preview, {
                        'attr': {
                            'class': 'btn btn-line-s',
                        }
                    })
                }}

                <input type="submit" value="Enregistrer" class="btn btn-line-s save no-ajax" data-save='{% if app.user and is_granted("ROLE_ADMIN") %}Vous pourrez compléter et publier votre espace depuis la rubrique "Gestion des espaces".{% else %}Vous pourrez compléter votre saisie depuis la rubrique "Mes espaces", puis la soumettre pour validation.{% endif %}'>

               {{ form_widget(
                    form.publish, {
                        'label': is_granted('ROLE_ADMIN') ? 'Publier' : 'Soumettre pour validation',
                        'attr': {
                            'class': 'btn btn-line-s js-publish no-ajax',
                            'data-confirm': is_granted('ROLE_ADMIN')
                                ? "ADMIN : Êtes-vous sûr de vouloir publier cet espace ?\n\nL'espace sera immédiatement visible par tous les candidats et le propriétaire sera notifié par email."
                                : "Êtes-vous sûr de vouloir soumettre cet espace pour validation ?\n\nUn administrateur vérifiera votre espace et le publiera. Vous serez notifié par email une fois l'espace publié."
                        }
                    })
                }}
            </div>
        </div> <!--end row btns-->


<div style="display:none;">{{ form_rest(form) }}</div>
{{ form_end(form) }}

</div>
</div>
</div>




<div class="content plusinfos margin">
    <div class="container">
        <div class="row pu-home-content info">
            <div class="col-xs-12 col-md-8 col-lg-8">
                <h4>Vous souhaitez obtenir plus d'informations</h4>
            </div>
            <div class="col-xs-12 col-md-4 col-lg-4  btn-right">
                <a href='https://www.plateau-urbain.com/contactez-nous/' target="_blank" class='btn btn-line'><span>Contactez-nous</span></a>
            </div>
        </div>
    </div>
</div>

<script>
(function () {
    // Mapping section id -> index de l'indicateur (0-based)
    var sections = ['one', 'two', 'three', 'four', 'five'];

    // Chercher la première section contenant au moins un champ en erreur
    var targetSection = null;
    var targetIndex   = -1;

    for (var i = 0; i < sections.length; i++) {
        var section = document.getElementById(sections[i]);
        if (section && section.querySelector('.has-error')) {
            targetSection = section;
            targetIndex   = i;
            break;
        }
    }

    if (!targetSection) {
        return; // Aucune erreur de validation inline, rien à faire
    }

    // Mettre en évidence l'indicateur latéral correspondant
    var indicators = document.querySelectorAll('.bkg .indicator');
    indicators.forEach(function (el) { el.classList.remove('active'); });
    if (indicators[targetIndex]) {
        indicators[targetIndex].classList.add('active');
    }

    // Scroll doux vers la section fautive
    targetSection.scrollIntoView({ behavior: 'smooth', block: 'start' });
})();
</script>



