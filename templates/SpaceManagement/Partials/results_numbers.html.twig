Résultats {{ 1 + (pagination.currentPageNumber - 1) * pagination.itemNumberPerPage }} - {{ ((pagination.currentPageNumber - 1) * pagination.itemNumberPerPage) +  pagination | length }} sur {{ pagination.getTotalItemCount }} {{ label }}.