

{% block content %}
{{ form_errors(form) }}
    <div class="panel panel-success">
        <div class="panel-heading">
            <h2 class="panel-title">Inscription</h2>
        </div>
        <div class="panel-body">
            <form action="{{ path('fos_user_registration_register') }}" method="POST" class="fos_user_registration_register form-horizontal" {% if form.vars.multipart %}enctype="multipart/form-data"{% endif %}>
                <div class="form-group">
                    {{ form_label(form.wishedSize, null, { 'label_attr': {'class': 'col-lg-3 control-label'} })}}
                    <div class="col-lg-9">
                        {{ form_widget(form.wishedSize) }}
                    </div>
                </div>
                <div class="form-group">
                    {{ form_label(form.category , null, { 'label_attr': {'class': 'col-lg-3 control-label'} })}}
                    <div class="col-lg-9">
                        {{ form_widget(form.category) }}
                    </div>
                </div>
                <div class="form-group">
                    {{ form_label(form.email , null, { 'label_attr': {'class': 'col-lg-3 control-label'} })}}
                    <div class="col-lg-9">
                        {{ form_widget(form.email) }}
                    </div>
                </div>
                {% for passwordField in form.plainPassword %}
                    <div class="form-group">
                        {{ form_label(passwordField , null, { 'label_attr': {'class': 'col-lg-3 control-label'} })}}
                        <div class="col-lg-9">
                            {{ form_widget(passwordField) }}
                        </div>
                    </div>

                {% endfor %}

                {{ form_rest(form) }}

                <div class="form-actions">
                    <input type="submit" value="{{ 'registration.submit'|trans({}, 'FOSUserBundle') }}" class="btn btn-success pull-right" />
                </div>
            </form>
        </div>
    </div>
{% endblock content %}
