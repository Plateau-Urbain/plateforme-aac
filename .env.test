# Variables d'environnement pour PHPUnit (APP_ENV=test)
# Note : .env.local n'est PAS chargé en env test. Copiez DATABASE_URL dans .env.test.local
# (généré automatiquement par scripts/prepare_test_database.sh depuis .env.local).
KERNEL_CLASS='App\Kernel'
APP_SECRET='$ecretf0rt3st'
MAILER_DSN=null://null
